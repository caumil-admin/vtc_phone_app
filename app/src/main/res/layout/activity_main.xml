<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000"
    android:padding="12dp">

    <!-- ===== 설정 영역 (데모 모드에서 숨김) ===== -->
    <LinearLayout
        android:id="@+id/boxConfig"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="VTC-ASSURE"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:textStyle="bold" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <EditText
                android:id="@+id/etServerIp"
                android:layout_width="0dp"
                android:layout_weight="3"
                android:layout_height="wrap_content"
                android:hint="서버 IP"
                android:inputType="text"
                android:textColor="#FFFFFF"
                android:textSize="14sp" />

            <EditText
                android:id="@+id/etServerPort"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:hint="포트"
                android:inputType="number"
                android:textColor="#FFFFFF"
                android:textSize="14sp" />
        </LinearLayout>

        <EditText
            android:id="@+id/etSn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="clientSecret"
            android:inputType="text"
            android:textColor="#FFFFFF"
            android:textSize="13sp" />

        <Button
            android:id="@+id/btnAuthFile"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="인증 파일 선택 (snEncrypt)"
            android:textSize="12sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button android:id="@+id/btnGlasses" android:layout_width="0dp"
                android:layout_weight="1" android:layout_height="wrap_content"
                android:text="안경 연결" android:textSize="12sp" />
            <Button android:id="@+id/btnServer" android:layout_width="0dp"
                android:layout_weight="1" android:layout_height="wrap_content"
                android:text="서버 연결" android:textSize="12sp" />
            <Button android:id="@+id/btnTest" android:layout_width="0dp"
                android:layout_weight="1" android:layout_height="wrap_content"
                android:text="표시 테스트" android:textSize="12sp" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button android:id="@+id/btnActivate" android:layout_width="0dp"
                android:layout_weight="1" android:layout_height="wrap_content"
                android:text="활성화" android:textSize="12sp" />
            <Button android:id="@+id/btnReconnect" android:layout_width="0dp"
                android:layout_weight="1" android:layout_height="wrap_content"
                android:text="재연결" android:textSize="12sp" />
            <Button android:id="@+id/btnDiag" android:layout_width="0dp"
                android:layout_weight="1" android:layout_height="wrap_content"
                android:text="지연 초기화" android:textSize="12sp" />
        </LinearLayout>
    </LinearLayout>

    <!-- ===== 상태 한 줄 ===== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <TextView
            android:id="@+id/tvGlassStatus"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="안경: 미연결"
            android:textColor="#9E9E9E"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/tvServerStatus"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="서버: 미연결"
            android:textColor="#9E9E9E"
            android:textSize="12sp" />

        <Button
            android:id="@+id/btnDemo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:paddingLeft="12dp"
            android:paddingRight="12dp"
            android:text="데모"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- ===== M3 경보 배너 ===== -->
    <TextView
        android:id="@+id/tvAlert"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="18dp"
        android:gravity="center"
        android:textSize="30sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="#B00020"
        android:visibility="gone" />

    <!-- ===== 지시 원문 ===== -->
    <TextView
        android:id="@+id/tvInstruction"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:textColor="#64B5F6"
        android:textSize="15sp"
        android:visibility="gone" />

    <!-- ===== 자막 ===== -->
    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="10dp"
        android:gravity="top"
        android:minLines="3"
        android:textColor="#FFFFFF"
        android:textSize="26sp" />

    <!-- ===== 로그 (데모 모드에서 숨김) ===== -->
    <ScrollView
        android:id="@+id/svLog"
        android:layout_width="match_parent"
        android:layout_height="160dp">

        <TextView
            android:id="@+id/tvLog"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:textColor="#9E9E9E"
            android:textSize="10sp" />
    </ScrollView>
</LinearLayout>
